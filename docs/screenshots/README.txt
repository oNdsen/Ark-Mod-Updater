Drop the four app screenshots here (PNG, ideally ~1100px wide, dark theme)
so the README image grid works:

  mods.png     - the Mods tab (table with thumbnails + preview card)
  server.png   - the Server tab (launch options + status card)
  config.png   - the Config tab (searchable settings list)
  update.png   - a running update (console + per-mod statuses)
