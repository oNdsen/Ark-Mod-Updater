#include "amucore/version.h"

namespace amucore {

// 2.x = the C++ rewrite (the AutoIt line ended at 0.9.9.x / updater 1.0).
const char* version() noexcept { return "2.2.1"; }

}  // namespace amucore
